from math import factorial
class Solution(object):
    def uniquePaths(self, m, n):
        """
        :type m: int
        :type n: int
        :rtype: int
        """
        res = factorial(m+n-2)/(factorial(n-1)*factorial(m-1))
        return res
